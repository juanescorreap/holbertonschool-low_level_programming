#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
* main - Prints the alphabet
* Return: always returns to 0
*/
int
main(void)
{
char ch;
for (ch = 'a' ; ch <= 'z' ; ch++)
putchar (ch);
for (ch = 'A' ; ch <= 'Z' ; ch++)
putchar (ch);
{
putchar (012);
}
return (0);
}
