#include "holberton.h"
/**
* _islower - Checks for a lower case character.
* Return: returns to zero.
* @c: Varable that will be checked for lowercase.
*/
int _islower(int c)
{
	if (c <= 122 && c >= 97)
	{
		return (1);
	}
	else
	{
	}
	return (0);
}
