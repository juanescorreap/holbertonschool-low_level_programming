#include "main.h"
/**
* _islower - checks for a lower case character
* Return: returns to zero
* @c: variable is varable that will be checked for lowercase
*/
int _islower(int c)
{
	if (c <= 122  && c >= 97)
	{
		return (1);
	}
	else
	{
	}	return (0);
	}
